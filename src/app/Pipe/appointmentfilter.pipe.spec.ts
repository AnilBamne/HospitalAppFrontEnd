import { AppointmentfilterPipe } from './appointmentfilter.pipe';

describe('AppointmentfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AppointmentfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
